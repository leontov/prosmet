# Screenshots

Real Chromium screenshots are generated only after the Primary E2E run. Required filenames are intentionally not faked; the workflow evidence will populate this directory in a later commit.
