name: Prosmet live 3200 server-first (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical production deployment
        run: |
          echo "This duplicate workflow is retired."
          echo "The canonical release is verified by .github/workflows/pr-verify.yml."
          echo "After merge, production is deployed only by .github/workflows/launch-3200.yml."
