name: PR7 Trusted PostgreSQL Gate (manual archive)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical gate
        run: |
          echo "The automatic duplicate PR7 gate is retired."
          echo "The canonical release gate is .github/workflows/pr-verify.yml on prosmet-primary."
          echo "This workflow remains manual only for historical traceability."
