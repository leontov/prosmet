# ProSmet production release trigger

This release-only branch requests deployment of the exact current `main` revision:

`aee966c356377be3d2e676679912bd705907c1ae`

Scope: merged desktop and mobile web visual polish, resizable GPT-like workspace canvases, theme controls, responsive mobile reference UI, and branded in-canvas PDF preview.

The branch must not be merged. It exists only to invoke the protected production workflow against the pull request base SHA and must be closed after the production ledger records an exact-SHA result.
