# Greenfield V3 production release

This release request contains no application changes.

The protected production workflow must checkout and deploy the pull request base SHA from `main`, verify `ui: greenfield` on the public health endpoint, and pass desktop/mobile Chromium against `https://kolibriai.online`.
