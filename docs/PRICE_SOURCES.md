# Price source policy

Priority: personal → organization → approved previous estimate → supplier → regional aggregate → licensed official → verified external → visible assumption → unknown.

Every item stores source label/type, region, date, VAT, delivery, confidence and confirmation status. The current starter catalog uses only `assumption` sources and is deliberately marked low confidence.
