# Production trigger

This commit exists only to exercise the canonical `main` push deployment path after the assistant-ui production gate fix.

Expected path: `main` push → Prosmet Greenfield Production → verify → E2E → immutable deploy → public HTTPS acceptance.
