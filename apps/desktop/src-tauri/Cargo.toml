[package]
name = "prosmet-desktop"
version = "0.1.0"
description = "Prosmet Greenfield desktop shell"
edition = "2024"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2.9.0", features = [] }
prosmet-estimate-engine = { path = "../../../crates/estimate-engine" }
