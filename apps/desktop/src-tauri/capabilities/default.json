{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege core capability for the ProSmet main desktop window. No shell, unrestricted filesystem or arbitrary URL permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
